{
  "scripts": {
    "build": "npx tailwindcss -i ./tailwind-input.css -o ./public/results/tailwind-output.css",
    "watch": "npx tailwindcss -i ./tailwind-input.css -o ./public/results/tailwind-output.css --watch"
  },

  "devDependencies": {
    "tailwindcss": "^3.3.2"
  }
}
